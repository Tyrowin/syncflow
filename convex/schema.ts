import { defineSchema } from "convex/server";

// Initial empty schema. Will be extended in Task 2.1.
// Adding this file removes the TypeScript warning about no input files.
export default defineSchema({
  // Projects, columns, tasks tables to be added next.
});
